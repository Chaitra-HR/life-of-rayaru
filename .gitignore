# OS / editor cruft
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store
*.swp
*~

# dependencies (the site vendors what it needs under antaranga/vendor)
node_modules/

# the build output (npm run build → dist/)
dist/

# local build output and environment
*.log
npm-debug.log*
.env
.env.*
